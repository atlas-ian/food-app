import { CartPage } from '@/components/cart/CartPage';

export const metadata = {
  title: 'Your Cart - FoodApp',
  description: 'Review your order and proceed to checkout',
};

export default function Cart() {
  return <CartPage />;
}